import React from "react";
import { CreateForm } from "../../components/form/CreateForm";

export const Create = () => {
    return (
        <div>
            <h1>Create swipe page</h1>
            <CreateForm />
        </div>
    );
};
